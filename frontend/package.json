{
  "name": "ets2-translator-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Translator V2 前端 SPA — 由 native 宿主 WebView2，经 REST + SSE 与 Go 通信（ADR-016）",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vue-tsc": "^3.3.11"
  },
  "dependencies": {
    "vue": "^3.5.42"
  }
}
